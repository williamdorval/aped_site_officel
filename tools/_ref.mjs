/* Ancien nom, gardé le temps qu'une session en cours finisse.
   L'outil vit maintenant dans `tools/refs-releve.mjs` — hors du
   namespace `tools/_*`, qui est ignoré par git parce qu'il est réservé
   aux SORTIES de mesure. Un outil durable n'y a pas sa place. */
import "./refs-releve.mjs";
